
  # App Initialization

  This is a code bundle for App Initialization. The original project is available at https://www.figma.com/design/AsB0f7oWVRjRyrpSrsqOlg/App-Initialization.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  